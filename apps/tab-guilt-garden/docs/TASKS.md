# TASKS — タブ庭園 本実装

対応: [SPEC.md](./SPEC.md) / [PLAN.md](./PLAN.md)

規律: **1タスク = 1コミット**。完了したら即 `[x]` に更新してから次へ進む。

---

## フェーズ0: 準備

- [x] **T0** `frontend-design` と `visual-qa` スキルを呼び出し、UI着手前に指針を取り込む
      （実装コミットなし。設計方針を docs に反映する場合のみコミット）

## フェーズ1: 基盤とドメイン

- [x] **T1** テスト基盤の導入
      Vitest + jsdom + v8カバレッジ。Node 25 の localStorage を無効化する setup を用意。
      `npm run test` / `npm run test:coverage` を追加。カバレッジ閾値80%を設定。
      → 検証: 空のスモークテストが通る

- [ ] **T2** `domain/` へ移設し `husk` / `fossil` 段階を追加 (FR-103)
      `types.ts` `constants.ts` `health.ts` `format.ts` `species.ts` を `src/domain/` へ移動。
      `Stage` に `husk` `fossil` を追加、`computeStage` を放置実時間ベースで拡張。
      → 検証: health/format/species の単体テスト（境界値・時刻巻き戻り E-10 含む）

- [ ] **T3** `infra/storage.ts` の作り直し (FR-300/301/302)
      機能検出、メモリフォールバック、要素ごとのスキーマ検証、墓標200件上限、
      クォータ超過時の切り詰め再試行。
      → 検証: 不正JSON・不正スキーマ・容量超過・書き込み不能の各異常系テスト

- [ ] **T4** `domain/garden.ts` — 庭の状態遷移を純粋関数化 (FR-203/204/205/206)
      自レコードのみ更新、ゴースト掃除、ID重複排除つき埋葬、埋葬済みIDの自己修復。
      → 検証: 同一ゴーストの二重埋葬(E-06)・二重close(E-07)・他タブ入力の保護(E-09)

- [ ] **T5** `domain/ledger.ts` — 通算記録 (FR-400)
      単調非減少の更新関数、焼き払い時の保持、明示トグルでの全消去。
      → 検証: 焼き払い後も通算値が減らないこと(AC-400a/b)

- [ ] **T6** `domain/rank.ts` + `domain/achievements.ts` (FR-401/402)
      階級ラダーと次段階までの進捗、実績10種の解除判定（純粋関数）。
      → 検証: 各階級の境界値、実績の解除条件と不可逆性

- [ ] **T7** `engine.ts` — GardenEngine（依存注入）
      `now` / `selfId` / `store` / `channel` / `isFocused` を注入。
      tick・埋葬・焼き払い・実績解除通知を統括。
      → 検証: 注入時計を進めての枯死・ゴースト埋葬・焼き払い挙動

- [ ] **T8** `main.ts` を薄い配線に差し替え
      本物の依存を組んで Engine を駆動するだけにする。`pageshow` での bfcache 復帰対応(E-08)。
      → 検証: ビルドが通り、既存のコアループがブラウザで動作する

## フェーズ2: UI

- [ ] **T9** `ui/` へ分割
      `render.ts` を `garden-view.ts` / `graveyard.ts` / `scoreboard.ts` に分割。
      墓標に埋葬からの経過時間を追加 (FR-403)。
      → 検証: jsdom での描画テスト

- [ ] **T10** スコアボード — 階級進捗と実績スタンプ (FR-401/402)
      通算記録・階級・次段階までの進捗・実績一覧（未解除も条件が見える）。
      → 検証: 描画テスト、AC-401a（最高階級の表示）、AC-402b

- [ ] **T11** 初回説明パネル (FR-500/501/502)
      3ステップ図、非ブロッキング、再表示ボタン、ヘッダーの一文コピー、空状態の改善。
      → 検証: 初回表示・閉じたら記憶・再表示・焼き払いで再表示されない(AC-500d)

- [ ] **T12** 実績トースト (FR-402/602)
      新規解除時に1回だけ、`role="status"`。
      → 検証: 同一実績で再通知しないこと(AC-402a)

- [ ] **T13** `husk` / `fossil` の視覚表現 (AC-600b/AC-103a)
      専用配色・傾き・コピーを既存トークンのみで追加。`dead` と識別可能にする。
      ストレージ退避時の告知行 (AC-300b) もここで追加。
      → 検証: 段階ごとの data-stage 適用テスト、コントラスト計測

## フェーズ3: 仕上げと検証

- [ ] **T14** README とドキュメント更新 (FR-503)
      コアループ・放置ゲームとしての側面・遊び方を冒頭で伝える。
      プロトタイプ由来の記述を本実装向けに改める。

- [ ] **T15** 最終検証
      build / テスト＋カバレッジ80% / docker build と起動確認と後片付け /
      ブラウザ複数タブ実機確認 / `visual-qa` 計測 / Anti-Template Policy 確認。
      → 不足があれば修正して再検証（このタスクは全項目が緑になるまで閉じない）

---

## 進捗メモ

（実装中に判明した判断・逸脱をここに追記する）
