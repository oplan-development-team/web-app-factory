import { defineConfig } from 'vite';

// base: './' is required because GitHub Pages serves this app from the
// sub-path /web-app-factory/redacted-exif/.
export default defineConfig({
  base: './',
});
