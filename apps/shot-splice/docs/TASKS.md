# TASKS — SHOT SPLICE 本実装

対応: [SPEC.md](./SPEC.md) / [PLAN.md](./PLAN.md)

規律: **1 タスク = 1 コミット**。完了したら即 `[x]` に更新してから次へ進む。

---

## フェーズ 0: 設計

- [x] **T0** `frontend-design` を適用し、視覚方向（配色・タイポグラフィ・構図・モーション）を SPEC §7 に確定する
      → 検証: SPEC §7 に具体値が入っている（「クリーンでミニマル」のような曖昧な記述がない）

## フェーズ 1: 足場

- [x] **T1** プロジェクト足場
      Vite + TS(strict) + Vitest(jsdom, v8 coverage 80%) + `deploy.json` + Dockerfile + `.dockerignore` + `.gitignore`。
      `vite.config.ts` に `base: './'`。
      → 検証: `npm run build` と空のスモークテストが通る

## フェーズ 2: コアアルゴリズム（TDD）

- [x] **T2** `core/types.ts` / `core/gray.ts`
      `GrayImage` 型、行の平均絶対差プリミティブ（全行版 / 等間隔サンプリング版）、テスト用の擬似乱数グレー画像ビルダー。
      → 検証: サンプリング版が全行版と整合すること、境界（h=0, h>height）の扱い

- [ ] **T3** `core/alignment.ts` — 重なり検出 (FR-101〜106)
      粗探索（幅縮小のみ・行サンプリング）→ 精密探索（フル解像度・1px 刻み・全行）。`matched` 判定。
      → 検証: AC-101 / AC-102 / AC-103 / AC-104（平均化実装なら落ちる回帰テスト）/ AC-106 / E-12

- [ ] **T4** `core/banding.ts` — 共通ヘッダー/フッター検出 (FR-201〜203)
      上端・下端からの連続一致行数、25% クリップ、1 枚以下は 0/0。
      → 検証: AC-201 / AC-202 / AC-203 / AC-204 / E-07

- [ ] **T5** `core/layout.ts` — 多枚数レイアウト計算 (FR-205/206, FR-502)
      カット適用範囲の決定、`maxOverlapPx` 算出、重なりクランプ、配置 y と出力サイズ。
      → 検証: AC-301 / AC-302 / E-01 / E-06 / E-12、`trimEnds` の両モード

- [ ] **T6** `core/naming.ts` ほか小物
      出力ファイル名、一致度ラベル分類、Canvas 面積上限判定 (FR-503, FR-403, FR-505)。
      → 検証: 境界値（閾値ちょうど）のラベル、面積上限ちょうど

## フェーズ 3: Canvas 層

- [ ] **T7** `imaging/canvas.ts` / `imaging/grayscale.ts`
      画像→Canvas、上下カット、幅スケール、Canvas→`GrayImage`（フル / 幅のみ縮小）。
      narrow interface（`CanvasLike` / `Ctx2DLike` / `CanvasFactory`）を定義する。
      → 検証: フェイク Canvas でのソース矩形・スケール引数の検証

- [ ] **T8** `imaging/compose.ts` — 合成描画
      Layout に従った通常合成、継ぎ目ルーペ（等倍切り出し）、差分描画 (FR-401/402, FR-501/504)。
      → 検証: 描画順（前面レイヤ切替）、差分帯の座標、フル/プレビューの解像度分岐

## フェーズ 4: UI

- [ ] **T9** デザイントークンと基盤 CSS (SPEC §7)
      `tokens.css` / `base.css`。配色・角丸・余白リズム・タイポスケール・ノイズ・safe-area・reduced-motion。
      → 検証: ビルドが通り、トークンが 1 箇所に集約されている

- [ ] **T10** `ui/store.ts` — アプリ状態
      イミュータブル更新、購読、shots / seams / bands / activeSeam / diffMode / status。
      → 検証: 追加・削除・並べ替え時の seams 再構成、手動編集値の保持 (FR-207)

- [ ] **T11** `ui/intake.ts` — 読み込み (FR-001〜004, FR-008, FR-009, E-02/E-03/E-08)
      ファイル選択・ドラッグ&ドロップ・デコード・上限 12 枚・スキップ通知・幅正規化通知。
      → 検証: 非画像混入・上限超過・デコード失敗時の分岐

- [ ] **T12** `ui/filmstrip.ts` — ショット一覧 (FR-005, FR-006a/b, FR-007)
      アスペクト比可変サムネイル、splice line、削除、◀▶ 入れ替え、長押しドラッグ並べ替え、全クリア確認。
      → 検証: 並べ替えの境界（先頭/末尾）、長押し閾値（時刻注入）、E-10

- [ ] **T13** `ui/bandCard.ts` — 共通帯の提示と調整 (FR-204〜207, AC-205)
      検出値表示、適用トグル、手動微調整、`trimEnds` 切替、再検出時の差異提示。
      → 検証: 手動編集後に再検出しても上書きされないこと

- [ ] **T14** `ui/seamCard.ts` + `ui/seamSheet.ts` — 継ぎ目カードと調整シート (FR-301〜304, FR-403, FR-108)
      一致度バー（色補間）、大 mono readout、ボトムシート、ルーペ、Pointer ドラッグ、ステッパー、数値入力、矢印キー、前面レイヤ。
      → 検証: クランプ、キーボード操作、Pointer 経由とプログラム経由の一致 (AC-403)

- [ ] **T15** `ui/stage.ts` — プレビューステージ (FR-501/502)
      sticky ステージ、縮小プレビュー描画、平均色グロー、出力サイズ readout、差分モード反映。
      → 検証: 縮小率の算出、ショット 0/1 枚時の表示

- [ ] **T16** `ui/toolbar.ts` + 一括自動検出 (FR-107)
      一括検出の進捗表示、rAF による分割、PNG 書き出し (FR-503〜505)、面積上限警告。
      → 検証: 進捗の逐次更新、書き出し失敗時のエラー表示

- [ ] **T17** `main.ts` 配線と状態表示 (FR-601〜606)
      空 / 1 枚 / ローディング / エラー / 成功 の各状態、`aria-live` ライブリージョン、モーション適用。
      → 検証: 各状態が DOM 上で切り替わる

## フェーズ 5: 検証と仕上げ

- [ ] **T18** カバレッジ 80% 到達 (NFR-007)
      不足箇所を補うテスト追加。
      → 検証: `npm run test:coverage` が閾値を満たす

- [ ] **T19** `visual-qa` による実測検証 (AC-505, NFR-010)
      境界線の知覚可能性、line-height 継承、意図した視覚効果（一致度の色補間・グロー・階層）の数値確認。
      → 検証: 実測ログを残し、不合格箇所を修正する

- [ ] **T20** 実機相当ビューポート検証 (AC-401〜406)
      375 / 390 / 428 / 1280 で横スクロール 0、タップターゲット 44px 以上、コンソールエラー 0、
      3 枚読み込み→一括検出→PNG 書き出しの通し。
      → 検証: Playwright スクリプトの全項目 PASS

- [ ] **T21** README / Docker / Pages 確認 (NFR-008/009, AC-501/503/504)
      README（機能・アルゴリズム・スタイル方向・開発手順・Docker）、`docker build` の実行確認、
      `deploy.json` とサブパス配信の確認。
      → 検証: `docker build` 成功、サブパス配信でアセットが読める
