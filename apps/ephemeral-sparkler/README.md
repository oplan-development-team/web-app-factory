# 線香花火 — Ephemeral Sparkler

長押しで線香花火に火を灯し、蕾→牡丹→松葉→散り際と自前実装の物理で燃え尽きるまでを見守り、消えた瞬間の光跡だけを一枚のPNGとして持ち帰るプロトタイプです。

app-factoryパイプラインによる自律生成プロトタイプです（コンセプト立案からデザイン・実装・検証までエージェントが一括で行いました）。

## 遊び方

1. 画面下寄りの見えない判定エリア（花火の手元側）を長押しすると火が灯ります。
2. 押し続けている間だけ燃焼が進行します（合計約45秒、蕾→牡丹→松葉→散り際の4段階）。
3. 指を離すと、その時点で失火して消えます。最後まで見届けると自然に燃え尽きます。
4. 消えた瞬間、燃焼中の光を長時間露光のように蓄積した一枚の光跡画像がプレビューされます。「記念写真として保存」でPNGとしてダウンロードできます（iOS Safari等では画像を長押しして保存するフォールバック導線を表示します）。
5. 「もう一度、灯す」で新しい花火に再挑戦できます。

保存・再現の手段はPNGダウンロードのみです。シミュレーション状態の保存や再生機能は、「その場限り」というコンセプト上、意図的に持たせていません。

## 技術構成

- Vite + TypeScript、素のCanvas 2D API（描画・パーティクルライブラリ非依存）
- パーティクル物理・燃焼曲線ロジックは `src/sparkler-physics.ts` にUI/描画コードから分離して実装
- リアルタイム描画用キャンバスとは別に、DOMに接続しないオフスクリーンキャンバスへ発光パーティクルを`globalCompositeOperation: 'lighter'`で蓄積し、消火時にそのままPNG書き出し
- バックエンドなし、完全クライアントサイド完結

## 開発

```bash
npm install
npm run dev      # 開発サーバー
npm run build    # 型チェック + 本番ビルド（dist/ に出力）
npm run preview  # ビルド結果のプレビュー
```

## Docker

```bash
docker build -t ephemeral-sparkler .
docker run --rm -p 8080:80 ephemeral-sparkler
# http://localhost:8080/ で確認
```

## デプロイ

`deploy.json` で `pages: true` を指定しており、GitHub Pagesプレビュー対象です（`vite.config.ts` の `base: './'` によりサブパス配信に対応）。
