# 技術フック・観点リファレンス

このディレクトリのアプリが「画像アップロード→変換」「入力→装飾的なポスター出力」のパターンに偏りがちだったため、入力の入り口（何がアプリの核になる仕組みか）を広げる目的で作ったユーザー用の参考メモです。**自動生成パイプライン（app-factory）からは参照させていない**——アイディアの多様性は実際、この一覧なしでも自然に出てきているため、機械的に組み込む必要は今のところ無いという判断。

新しいアプリを考えるときに気分転換に眺める用のリストとして置いてある。固定の答えではなく発想の起点。

## 入力モダリティ別の技術フック

### 音声・マイク（Web Audio API）
音量・ピッチの解析は先行例がある。未開拓の切り口: リズム/テンポのタップ検出、ハミングからのメロディ採譜、環境音レベルの記録・可視化。

### キー入力・打鍵（keydown/keyup イベント）
打鍵間隔の解析は先行例がある。未開拓の切り口: 打鍵を音符に変換する作曲トイ、1日の打鍵ヒートマップ、打鍵の癖から生成する「デジタル筆跡」。

### マウス・ジェスチャー
一筆書き・自由描画は先行例がある。未開拓の切り口: カーソルの速度・加速度から生成するジェネラティブアート、物理演算を使ったフィジェットトイ、カーソル軌跡を使ったミニゲーム。

### 端末センサー（DeviceOrientation / DeviceMotion、モバイル）
未開拓の切り口: 水平器、傾けて転がすマーブル迷路、「振って○○」系の儀式アプリ、歩数・加速度センサーを使ったリズムトイ。iOS 13+は`requestPermission()`の明示的なユーザー操作起点の許可フローが必須な点に注意。

### クリップボード（Clipboard API）
貼り付けテキストの断片化・詩化は先行例がある。未開拓の切り口: セッション中にコピーした全履歴を時系列コラージュにする。

### タブ・ウィンドウ状態（BroadcastChannel、Page Visibility API）
複数タブ間の状態共有は先行例がある。未開拓の切り口: 複数タブ同期ゲーム、離席時間トラッキング日記、ウィンドウリサイズに反応するジェネラティブレイアウト。

### 日付・時刻（外部APIなしで完結する暦・天文計算）
星空再現・締切カウントダウンは先行例がある。未開拓の切り口: 「今日の残り時間」ビジュアライザ、月齢カレンダー、複数タイムゾーンを軌道図で見せる世界時計。

### ローカルストレージ／IndexedDB（「記憶」を持つアプリ）
未開拓の切り口: 開くたびに育つ庭（日課トラッカー＋ジェネラティブフィードバック）、日々変化する箱庭・砂紋、放置系インクリメンタルゲーム（開いていない間の経過時間も計算に含める設計にすると単なるクリックゲーム化を避けられる）。

### 音声合成／音声認識（Web Speech API、APIキー不要）
未開拓の切り口: テキストを読み上げるグリーティングカード、口述筆記から詩を生成するツール。

### 物理演算（自前実装 or matter.js等の軽量ライブラリ）
波紋・浅水シミュレーションは先行例がある。未開拓の切り口: 満足感重視の「瓶に物を落とす」トイ、ニュートンのゆりかごカスタマイズ、布・ロープのverlet積分シミュレーション。

### キー不要の公開データAPI
外部有料APIキーが前提のアイディアは避ける方針だが、**認証不要・無料枠が明確なAPI**（Open-Meteo気象API、sunrise-sunset.org、USGS地震データ等）を使ったリアルタイムデータ駆動のジェネラティブアートは検討対象にしてよい。採用する場合はAPIの利用規約・レートリミット・可用性をidea-scout段階で軽く確認しておく。

### 画像以外のファイルドロップ
画像アップロード系は複数の先行例があり手薄ではない。CSV/テキストファイルのドロップからの可視化、`.ics`カレンダーファイルをドロップして「1年の形」を見せるポスター化など、非画像ファイルの切り口は未開拓。

### ルールベースの文章生成（LLM API不要、重み付きランダム・マルコフ連鎖等）
未開拓の切り口: おみくじ、言い訳ジェネレーター、バンド名ジェネレーターなど、決定的またはシード駆動の軽量な文章生成トイ。

### ゲーム・トイ系（生成物ポスター以外の出力）
装飾的ポスター生成の出力パターンは複数の先行例があり手薄ではない。小規模パズル、リズムゲーム、放置系インクリメンタルゲームなど、出力が「1枚の画像」ではなく「継続的な操作・進行」であるものは未開拓。

## 使い方の指針

- 1回の生成で全カテゴリを網羅する必要はない。ただし、直近の生成物が特定のカテゴリ（画像アップロード、装飾的ポスター出力）に偏っている場合は、意識的に別のカテゴリから選ぶ。
- カテゴリを選んだら、そのカテゴリの「未開拓の切り口」の例をそのまま採用するのではなく、New_Service_App固有の文脈（スタイル方向・題材選び）で具体化すること。
