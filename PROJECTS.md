# PROJECTS

単発・小規模Webアプリのアイディアと進行状況の一覧。運用ルールは `.claude/CLAUDE.md` を参照。

## 進行中

（現在進行中の案件なし）

## プロトタイプ

### スクショ継ぎ足しツール（3スタイル比較）

ユーザーが持っていた既存プロトタイプ（`~/Downloads/screenshot-stitcher.html`）のアイディアのみを引き継ぎ、見た目は参考にせずゼロから作り直した案件。2枚に分けて撮ったスクリーンショット（上半分・下半分）の重なりを自動検出（グレースケール化した行同士の差分を粗解像度→高解像度の2段階探索で最小化）し、手動ドラッグ・矢印キーでの微調整、固定ヘッダー/フッターのカット、差分表示モードでの位置合わせ精度確認、PNG書き出しに対応。機能要件は3つとも共通で、スタイル方向だけを変えて並行生成し、ユーザーがどれを採用するか比較検討できるようにした。

- **写真暗室・アナログ現像室版**（`apps/shot-splice-darkroom/`）: 赤いセーフライトの薄暗さ・コンタクトシート・ルーペでの継ぎ目確認というメタファーで統一。配色は暖色系の赤黒（既存のダークラグジュアリー系とは差別化）、Oswald/Space Mono/Caveatの3書体使い分け。
- **スイス／インターナショナル・スタイル版**（`apps/shot-splice-grid/`）: 測定・製図的な精度重視のグリッドデザイン。オフホワイト＋黒＋vermillion差し色1色、Space Grotesk＋JetBrains Mono、測定ブラケット風コーナー装飾。
- **ネオブルータリズム版**（`apps/shot-splice-brutal/`）: 太い黒枠・ベタ塗り原色・紙工作風オフセット影で実用ツールらしい荒々しさを表現。配色はコバルトブルー×アシッドライム×警告オレンジ（既存のtab-guilt-gardenとは配色で差別化）、Archivo Black＋JetBrains Mono＋Space Grotesk。

3つともprototype-builderエージェントが実装、実装前に`frontend-design`スキル、実装後に`visual-qa`スキルで検証済み。いずれも自己申告のコードレビューだけで終わらせず、ブラウザでの実操作（Playwright）で「コード上は正しく見えるが実際には機能していない」不具合を発見・修正している（`hidden`属性がCSSの詳細度衝突で無効化される問題が3つとも共通して見つかった。差分表示モードのblend対象レイヤー不一致、自動検出アルゴリズムの粗探索段階での誤検出、といった個別の実装バグも発見・修正済み）。

**メインセッションによる独立検証**: 3つとも`npm run build`・`docker build`〜`docker run`〜HTTP 200応答を独立に再現し、確認後はimage/containerとも削除済み。ブラウザで実際の画面も確認し、Anti-Template Policy（画一的なカードグリッド等の禁止パターンに非該当、3つとも既存カタログの同系統アプリとは配色・題材で明確に差別化できている）を満たしていることを確認した。暗室版はさらにPlaywrightでの実データ位置合わせ（既知重なり画像で自動検出値・手動ドラッグ後の値が更新されること）も確認済み。

**判断結果**: 3スタイルどれもデザインとしては採用せず、「アルゴリズムのみ引き継ぎ、デザインはiOSライク・モバイルファーストで新規に作り直す」方針で本実装（`apps/shot-splice/`、上記「進行中」参照）に進んだ。3つのディレクトリ自体は参考実装として残しており、削除するかどうかは本実装完了後にあらためて判断する。

### 液だまり（Puddle Tilt）（`apps/puddle-tilt/`）

app-factoryパイプラインが自律生成したプロトタイプ。端末を傾けると画面の中の水たまりが実際に流れ・波打ち、油膜のように虹色に光る、触って眺めるためだけの感覚トイ。Canvas 2D上でFloat32Arrayの高さ場（浅水シミュレーション、隣接4近傍平均×2−前フレーム値＋減衰係数）を自前実装し、DeviceOrientationEvent（beta/gamma）から導出した重力ベクトルを毎フレーム高さ場に加算して傾きに応じた水の移流を表現する。iOS 13+の`requestPermission()`に対応した明示的なオンボーディングを持ち、傾きセンサー非対応環境ではポインタドラッグへ自動フォールバックする（"POINTER ONLY"表示）。任意の瞬間をPNG書き出し可能。バックエンドなし、完全クライアントサイド完結。技術スタックはVite + TypeScript（フレームワーク非依存）。

スタイル方向はダークラグジュアリー。艶消しの黒〜チャコールのステージに水たまりだけがスポットライトを浴びた「宝石」のように浮かび上がる構図、水面は薄膜干渉風のhue-shift（ティール→マゼンタ→ゴールド）で彩色し、UIクロムはプラチナ/シャンパンゴールドの罫線1色に絞る。

Design QA（2体・2ラウンドとも合格）、Verify（`npm run build`・vitest 30件・Docker独立ビルド・375/768/1440幅でのPlaywright確認・傾きセンサー非対応環境でのフォールバック動作確認）はいずれも合格、コンソールエラーなし。

**独立検証結果（メインセッション実施）**: `npm ci`＋`npm run build`成功（JS gzip 5.16KB、CSS gzip 2.83KB）。`docker build --no-cache`〜起動〜HTTP 200を独立に再現し、確認後はimage/containerとも削除済み。ブラウザ実機でオンボーディング→「はじめる」→ポインタドラッグでの波紋発生を確認、コンソールエラー0件。見た目もダークラグジュアリー方向で作り込まれている。

既知の制約: `vitest`の依存先（`vite-node`経由のesbuild）に中〜重度のnpm audit勧告があるが、開発サーバー限定の脆弱性で本番ビルド（`dist/`）には影響しない。傾きセンサーの実機（スマートフォン）での動作は本セッションでは未確認（ヘッドレスChromeでのフォールバック動作のみ確認）。

**採否待ち**: 機能・デザイン・Dockerビルドまで一通り動作確認済み。採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する。

### 校正記号diffビューア（Proofmark Diff）（`apps/proofmark-diff/`）

app-factoryパイプラインが自律生成したプロトタイプ（夜間無人実行）。改稿前後の2つのテキストを貼り付けると、単なる色分けハイライトではなく、紙の校正で実際に使われる校正記号（削除=トルツメ風ループ、挿入=キャレット+余白注記、置換=取り消し線+ルビ風訂正、移動=手描き風の揺らぎを持つ矢印）の見た目で差分を可視化するツール。差分計算はMyers差分法・移動検出ヒューリスティックとも外部ライブラリなしの自前実装。文字単位/単語単位の切替、完成した校正刷りのPNG書き出しに対応。バックエンドなし、完全クライアントサイド完結。

スタイル方向はエディトリアル（活版印刷の校正刷り・原稿用紙の赤入れ原稿風）。生成りの紙色台紙に墨色インクの明朝体（Shippori Mincho）本文、朱色一色（装飾ではなく「赤入れ」の記号語彙として機能）の校正記号、手書き風フォント（Klee One/Kalam）の訂正注記、モノスペース（IBM Plex Mono）のUIラベルを対比させる。技術スタックはVite + TypeScript（フレームワークなし、バニラDOM + SVGオーバーレイ）。

Design QA（2体・いずれも合格、実機スクリーンショット確認済み）: 階層・余白リズム・重なり/影/モーションによる奥行き・タイポグラフィ対比・意味を持つ配色・作り込まれたhover/focus/active状態・非グリッド的な編集的構成・意味のあるモーションのいずれも満たしていると判定。Verify（独立検証・合格）: `npm run build`成功、dev/preview起動、Playwright/Puppeteerでの実機操作（サンプル読込→4種の校正記号すべて描画確認、文字/単語モード切替、PNG書き出し、空入力エラー表示）・375/768/1440幅のレイアウト確認とも問題なし、コンソールのJSエラー0件（favicon 404のみ）。**Dockerビルドも独立確認済み**（`docker build`〜`docker run`〜HTTP 200応答、確認後にimage/containerとも削除）。メインセッション自身でも`node_modules`/`dist`を消してからの再ビルドで独立確認済み。

**パイプライン運用上の注記（要フォロー）**: 今回の実行では`.claude/workflows/app-factory.js`のIdeate/Critiqueフェーズにバグがあり、採点トップ（このアイディア、平均7.25/10で採用ライン7を突破）だったにもかかわらずワークフローが`no-idea-survived`として異常終了した。原因は、idea-scoutが出したタイトル「校正記号 diff ビューア」（スペースあり）を、idea-criticが採点結果で「校正記号diffビューア」（スペースなし）に正規化して返し、`allIdeas.find(i => i.title === top.title)`の完全一致検索が失敗したこと。ワークフロースクリプト自体の修正（空白正規化した比較へのフォールバック等）を試みたが、`.claude/`配下ファイルの編集は無人実行环境でsensitive file保護の権限確認がブロックされ適用できなかった（過去のログファイルrename問題と同種の制約）。そのため今回はメインセッションがConcept以降のフェーズをAgent呼び出しで手動代行してプロトタイプを完成させた。**次回の対話セッションでこの完全一致マッチングのバグを`.claude/workflows/app-factory.js`に手動で修正することを推奨**（対話セッションならsensitive file確認を人間が承認できる）。

夜間無人実行のため、CLAUDE.mdの方針に従いmainには直接マージせず、`app-factory/auto-proofmark-diff`ブランチをpushし`gh pr create`でmain宛てPRを作成した。メインセッションによるレビュー（ビルド・Dockerビルド・Anti-Template Policy・実機確認）を経てマージ済み。

### 等高線ドローイング（Contour Draw）（`apps/contour-draw/`）

app-factoryパイプラインが自律生成したプロトタイプ。マウス/タッチで自由に線を描くと、ストローク経路に沿ったソフト円形カーネルで密度を高さ場（スカラー場）に加算堆積し、marching squaresを自前実装で等高線化。測量図・地形図風のポスターとしてPNG（高解像度）/SVG（ベクター）で書き出せるツール。線が重なる・往復する・円を描くほど地形が盛り上がる仕組みで、5本ごとの計曲線には架空の標高ラベルが付く。バックエンドなし、完全クライアントサイド完結。

スタイル方向はダークラグジュアリー（測量原図のブループリント意匠）。深いインク紺〜チャコール背景に白金/真鍮色の等高線、彫刻プレート風の広い字間セリフ（Cormorant）とスタンプ風モノスペース（IBM Plex Mono）を対比させる。配色プリセット3種（藍図Blueprint／測量原図Survey Buff／モノクロームInk）、用紙比率3種（A4縦/A3縦/正方形）に対応。技術スタックはVite + TypeScript（フレームワークなし、marching squares・SVG組み立てとも外部ライブラリ非依存）。

Design QA（2体・いずれも合格）: 階層・余白リズム・奥行き・タイポグラフィ対比・意味を持つ配色（3プリセットが実在の製図様式のメタファー）・作り込まれたhover/focus/active状態のいずれも満たしていると判定。Verify: `npm run build`成功、Playwrightでの実機操作（ループ描画→等高線生成、Undo、配色切替、PNG/SVG書き出し）・375/768/1440幅のレイアウト確認とも問題なし、コンソールエラー0件。**Dockerビルドも独立確認済み**（`docker build --no-cache`〜`docker run`〜HTTP 200応答、確認後にimage/containerとも削除）。

夜間の無人パイプライン実行がブランチをpushしたままmainへの反映を伴わず、レビュー待ちのまま放置されていたのをメインセッションが発見。独立worktreeで`npm run build`・`docker build`+起動+HTTP 200・Anti-Template Policyを再確認し、いずれも合格したためmainにマージした（2026-08-28）。

**採否待ち**: 機能・デザイン・Dockerビルドまで一通り動作確認済み。採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する。

### ⚠️ スノードーム・メーカー（`apps/snow-globe-maker/`、未マージ・`app-factory/snow-globe-maker`ブランチ）

**他の項目と異なり、Design QA・Verifyとも未実施・mainへ未マージの状態。** 好きな写真をアップロードするか、ミニキャンバスに小さなシーンを描くと、それがガラスドームの中に封じ込められた「スノードーム」として表示されるツール。デスクトップはドラッグ、モバイルはDeviceMotionで端末を振ると、ドーム内の雪・グリッター・紙吹雪がCanvas 2D自作物理（重力・減衰・境界反射）で舞う。気に入った瞬間をブラス罫線のポストカード風PNGとして書き出せる。バックエンドなし、完全クライアントサイド完結。

スタイル方向はライトラグジュアリー（アイボリー×ブラスのブティック・ギフト什器演出。既存のダークラグジュアリー系アプリとはあえてトーンを分けている）。技術スタックはVite + TypeScript（バニラ）+ Canvas 2D。

本パイプラインの実行は、2026-08-25〜26のバッチ検証で発生した「ログファイルリネームがsensitive file保護でrequires_action停止」個体のサルベージ再生成として開始されたが、**今回は別原因（`.claude/logs/app-factory/`へのログ書き込み直後、`CLAUDE_CODE_HOLD_UNANSWERED_PARKED_PERMISSION`によるものと推測される権限確認のパーク）でBuildフェーズ完了直後・Review & Fix開始前にワークフロー自体が完全停止し、2.5時間以上進行しなかった**。メインセッションはこの停止したサブエージェントの権限確認に応答する手段を持たないため復旧を諦め、ディスク上に残っていた実装物（`npm run build`はメインセッションが独立に成功を確認）をロストさせないよう、**mainではなく`app-factory/snow-globe-maker`ブランチにコミット・pushするだけに留めた**。Design QA（Anti-Template Policy適合）・Verify（実機動作・Dockerビルド）はいずれも未実施。

**要人手対応**: 採否判断の前に、(1)このブランチを見て実装内容を確認、(2)Design QA/Verifyを人力またはパイプライン再実行で補う、(3)問題なければmainへマージ、のいずれかが必要。ログファイル名が`unassigned-run.md`になっている（本来`<runId>.md`）のはこの実行中に発生した別の小さな不具合。

### クリップボード詩人（Clipboard Poet）（`apps/clipboard-poet/`）

app-factoryパイプラインが自律生成したプロトタイプ。セッション中にCtrl+V（Cmd+V）で貼り付けたテキスト断片をpasteイベントで拾い集め、貼るたびに感熱レシートプリンターのような紙が画面下方向に伸びていく。蓄積した断片は行分割・トリミング・シャッフルの3トグル（外部AIは使わず、断片の長さ・句読点・改行位置だけを手がかりにしたルールベース処理）で「見つけ詩（found poetry）」に組み替えられ、「詩を刷る」ボタンを押すたびに新しい乱数シードで違う詩が生成される再演性が体験の核。貼り付け内容・生成物はメモリ上のみで保持し、保存も送信もせずリロードで消える一期一会の演出。直近の詩だけはPNG書き出し・テキストコピーで持ち帰れる。

スタイル方向はレトロフューチャリズム。80〜90年代のPOSレジ／ラベルプリンター実機の筐体をスキューモーフィズムで再現（ベージュ〜パテグレーのABS樹脂パネル、LED赤色7セグメント風カウンター、アンバー/グリーンのバックライト付き物理トグルスイッチ3個、赤い大きな物理ボタン）。役割別に4フォント（見出しOswald、日本語UI用Zen Kaku Gothic New、LED表示用VT323、レシート印字用DotGothic16）を使い分け。技術スタックはVite + TypeScript（フレームワーク非依存）。

Design QA（2体・2ラウンドとも合格）: Anti-Template Policyの階層・余白リズム・奥行き・タイポグラフィ・配色・hover/focus/active状態のいずれも満たしていると判定。Verify（機能・ビルド・375/768/1440幅でのレイアウト）も合格、コンソールエラーなし。Dockerビルドも独立に確認済み（nginxコンテナで200応答を確認後、image/containerとも削除）。

夜間の無人パイプライン実行がブランチをpushしたままmainへの反映を伴わず、レビュー待ちのまま放置されていたのをメインセッションが発見。独立worktreeで`npm run build`・`docker build`+起動+HTTP 200・Anti-Template Policyを再確認し、いずれも合格したためmainにマージした（2026-08-28）。日本語フォント（Zen Kaku Gothic New）のunicode-rangeサブセットでCSSバンドルが約200KB（gzip後）とマイクロサイトのCSS予算を超過している点は、本実装移行時の見直し候補として記録しておく。

**採否待ち**: 機能・デザインとも動作確認済み。採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する。

### 声の地層（Voice Strata Poster）（`apps/voice-strata-poster/`）

app-factoryパイプラインが自律生成したプロトタイプ。マイクで録った声の音量・ピッチ・間（ポーズ）を、地質調査のボーリングコアのような縞地層としてリアルタイム解析・積層描画し、標本ラベル・深度スケール・岩相凡例つきの一枚の科学標本ポスターとしてPNG書き出しできる作品性重視アプリ。ピッチは6種の岩相（玄武岩・粘板岩・頁岩・砂岩・石灰岩・石英脈）に離散マッピングし、無音区間は「不整合層」として明確に区別する。Web Audio API（自前実装の自己相関ピッチ検出）+ Canvas 2D APIのみで完結し、外部ライブラリ・サーバー不要。

スタイル方向はエディトリアル（地質調査フィールドノート／自然史博物館の標本ラベル）。クラフト紙背景に鉱物パレット（玄武岩の黒灰・頁岩の青灰・砂岩の黄土・石灰岩のクリーム・石英脈のピンク白・粘板岩の深藍）、Frauncesイタリック見出し＋IBM Plex Monoの計器風ラベル＋Source Serif本文の3層タイポグラフィ。技術スタックはVite + TypeScript（フレームワーク非依存）。

Design QA（2体・2ラウンドとも合格）: Anti-Template Policyの階層・余白リズム・奥行き・タイポグラフィ・配色・hover/focus状態のいずれも満たしていると判定。Verify（機能・レイアウト）は375/768/1440幅・録音〜生成〜保存の一連のフローともに合格、コンソールエラーなし。**Dockerビルドのみ未検証**: このセッションのネットワークポリシーがDocker Hub CDN（production.cloudfront.docker.com / registry-1.docker.io）への接続を拒否するため、prototype-verifier・メインセッションの両方が独立に`docker build`を試行したが、ベースイメージ（node:22-alpine / nginx:alpine）の取得段階で失敗した（403 Forbidden・429 Too Many Requests、いずれも環境起因）。Dockerfile自体は他の成功実績があるアプリ（kamon-generator等）と同一ロジックで、コード上の不備ではないと判断している。Docker Hubへのアクセスが許可された環境での再確認が必要。

**採否待ち**: 機能・デザインは動作確認済みだが、Dockerビルドのみ環境要因で未検証。採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する（本実装移行時にDockerビルドも改めて確認すること）。

### 等高線ポートレート（Contour Portrait）（`apps/contour-portrait/`）

app-factoryパイプラインが自律生成したプロトタイプ。アップロードした顔写真・ペット・物などの明暗（輝度）を標高値に見立て、等高線（コンタライン）だけで描く測量図/地形図風のポートレート・ジェネレーター。画像を輝度マップに変換し、d3-contour（marching squares）で等高線を抽出してSVGとして描画する。等高線の本数・太さ・平滑化・階調反転をリアルタイム調整でき、単色モード（インク色+紙色プリセット）と多色モード（標高ハイプソメトリック配色: 古典地形図/ブループリント/セピアの3プリセット）に対応。四隅のトンボ・目盛りルーラー・標高カラーランプの凡例・タイトルラベルなど、額装されたポスターとしての体裁を持つ。PNG（2x/3x）・SVGの両方で書き出し可能。技術スタックはVite + TypeScript（フレームワーク非依存、バニラDOM）、完全クライアントサイド完結。

スタイル方向はスイス（インターナショナルタイポグラフィックスタイル）を測量図・技術製図寄りに具体化したもの（紙色#F5F3EE系背景+チャコール#1A1A1Aインク、Inter/IBM Plex Sansの字間を広げた大文字ラベル、数値情報はIBM Plex Monoで統一、角丸・ドロップシャドウ・グラデーション不使用、罫線はすべて1pxヘアライン）。Design QA（2体×2ラウンド）はいずれも合格、Verifyもビルド・起動・主要機能・375/768/1440幅のレイアウト・コンソールエラー0件を確認済み。

夜間の無人パイプライン実行がブランチをpushしたままmainへの反映を伴わず、レビュー待ちのまま放置されていたのをメインセッションが発見。独立worktreeで`npm run build`・`docker build`+起動+HTTP 200・Anti-Template Policyを再確認したところ、いずれも合格（**当時未検証だったDockerビルドも、今回の環境では問題なく成功・200応答を確認**。旧記録の「egress制約で未検証」はこのセッションの環境条件下の話であり、コード側の欠陥ではなかったことが裏付けられた）。これによりmainにマージした（2026-08-28）。

**採否待ち**: 動作確認済みだが、採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する。

### 標本図鑑プレート・ジェネレーター（`apps/specimen-plate-generator/`）

app-factoryパイプラインが自律生成したプロトタイプ。手元の植物・貝殻・小物などの写真をアップロードすると、Sobelエッジ検出とクロスハッチング処理で銅版画エングレービング風の線画に変換し、ラテン語風の学名ラベル・プレート番号・目盛り付きスケールバー・プロシージャル生成した経年紙テクスチャ（外部画像素材不使用）を添えた19世紀博物誌図版（natural history plate）としてPNG/SVG書き出しできるジェネレーター。バックエンドなし、完全クライアントサイド完結。

スタイル方向はエディトリアル（実在の博物誌図版フォーマットを厳密に踏襲：二重罫線の枠、プレート番号、イタリック体の学名風キャプション、目盛り付きスケールバー）。配色はセピア/インク黒＋アイボリー紙の2〜3色に絞り、書体はEB Garamond（本文・キャプション）とJetBrains Mono（操作パネル）で役割分担。技術スタックはVite + TypeScript（フレームワーク非依存）。

Design QA（2体）は合格（禁止パターン該当なし、階層・奥行き・タイポグラフィ・配色・hover/focus状態など必要品質を充足）。機能検証（npm install/build/dev/preview、Playwrightでの画像アップロード〜線画調整〜PNG/SVG書き出し、375/768/1440幅のレスポンシブ確認）もすべて合格、コンソールエラー0件。

なお、実装フェーズでprototype-builderが`frontend-design`スキルを呼び出そうとしたが本セッションの利用可能スキル一覧に存在せず呼び出せなかった（CLAUDE.mdの標準手順からの逸脱）。代わりにCLAUDE.mdのデザイン品質担保の観点に沿って配色・タイポグラフィ・レイアウトを自分で明文化した上で実装している。

夜間の無人パイプライン実行がブランチをpushしたままmainへの反映を伴わず、レビュー待ちのまま放置されていたのをメインセッションが発見。独立worktreeで`npm run build`・`docker build`+起動+HTTP 200・Anti-Template Policyを再確認したところ、いずれも合格（**当時未検証だったDockerビルドも、今回の環境では問題なく成功・200応答を確認**。旧記録の「egress制約で未検証」はこのセッションの環境条件下の話であり、コード側の欠陥ではなかったことが裏付けられた）。これによりmainにマージした（2026-08-28）。

**採否待ち**: 採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する。

### 架空の美術館キャプションジェネレーター（`apps/museum-caption-generator/`）

app-factoryパイプラインが自律生成したプロトタイプ。手元の物（コンビニ弁当の空き容器など）の写真をアップロードすると、現代アート展の壁面解説文風の大げさなキャプション（作品タイトル・作家名・年代・素材・寸法・評論文）を自動生成するネタツール。画像はCanvas APIで色相・明度・彩度のみを解析し（物体認識はしない）、あらかじめ用意した評論調の語彙バンクから重み付きランダムでキャプションを組み立てる。バックエンドなし、完全クライアントサイド完結。

スタイル方向はダークラグジュアリー・ギャラリー空間の再現（深いチャコール背景、スポットライト演出、真鍮フレーム＋クリーム台紙のキャプションカード、Playfair Display + Noto Serif JPの組み合わせ）。技術スタックはVite + TypeScript（フレームワーク非依存）。

Design QA・Verifyの両方が独立に、アップロード後もドロップゾーンが消えず展示画面に重なって表示される実装バグ（`.upload-section`のCSSが`[hidden]`属性を上書きしていた）を検出。メインセッションが原因を特定して修正し（`.upload-section[hidden] { display: none; }`を追加）、ブラウザで再確認して解消済み。

**採否待ち**: 動作確認済みだが、採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する。

### レシート詩集ジェネレーター（`apps/receipt-poem-generator/`）

app-factoryパイプラインが自律生成したプロトタイプ。品名・数量・金額を入力すると、感熱紙レシート風のプレビューに刷り上がり、「詩として読む」切替で数量・金額を隠して品名だけを縦一列に並べたfound poem（見つけた詩）に姿を変えるツール。合計欄のラベルや結びの一言は複数プリセットからシャッフルでき、Canvas 2Dで実際のレシートと同じ構造の高解像度PNGとして書き出せる。バックエンドなし、完全クライアントサイド完結。

スタイル方向はエディトリアル（原稿用紙風の入力欄＋額装展示風に傾けて掲示したレシートの2カラム構成）。生成り紙色地に墨色、朱色を合計金額・フォーカスリング・削除ボタン等に限定使用するアクセントとし、Shippori Mincho（明朝）とDotGothic16（ドットマトリクス）の書体対比で「手書きの詩」と「機械の印字」を演出する。技術スタックはVite + TypeScript（バニラ）+ Canvas 2D。

Design QA（2体とも合格）: 階層・余白リズム・奥行き（回転transform・shadow-soft・印字リビールアニメーション）・タイポグラフィ対比・意味を持つ配色・hover/active/focus-visible状態など、必要品質をほぼ全項目満たしていると判定。Verify（機能・レイアウト）: npm build成功、品目の追加/削除/並べ替え・シャッフル・詩モード切替・PNG書き出しが実機（Playwright）で動作、375/768/1440幅でレイアウト崩れなし、アプリ内JSエラー0件。**Dockerビルドのみ未検証**: このセッションのネットワークポリシーがDocker Hubレジストリへの到達を429 Too Many Requests（レートリミット）で拒否するため、prototype-verifier・メインセッションの両方が独立に`docker build`を試行したが、ベースイメージ（node:22-alpine）の取得段階で失敗した。修正ラウンドでDockerfileを他アプリと同じ標準パターン（node:22-alpine + package-lock.json同時COPY + npm ci）に統一済みで、記述上の不備は見当たらない。

なお今回の実行では、修正ラウンド後の再検証エージェント（prototype-builder）がStructuredOutputの返却に失敗しワークフロー自体がクラッシュしたため、Review & Fixループの2ラウンド目は完走していない。ただしDockerfile修正は既にディスクに反映済みで、修正ラウンド1の指摘（Dockerfile非標準パターン）は解消済み。メインセッションが独立に`npm run build`・`docker build`を再実行し、上記の結果を確認した上で本記録を作成している。

**採否待ち**: 機能・デザインは動作確認済みだが、Dockerビルドのみ環境要因で未検証。採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する（本実装移行時にDockerビルドも改めて確認すること）。

### レシート詩集ポスター（Receipt Poetry Scroll）（`apps/receipt-poetry-scroll/`）

app-factoryパイプラインがユーザー指定アイディア（seedIdea）から生成したプロトタイプ。買い物リスト・やることリストなど行区切りのテキストを貼り付けるだけで、感熱紙ロールを模した縦長ポスターに変換するツール。**既存の`apps/receipt-poem-generator/`とは機構が異なる**: あちらは品名・数量・金額をすべて手入力する方式だが、本アプリは金額を一切入力せず、各行のテキスト（文字数・文字の多様性）から決定性のあるハッシュ＋乱数シードで「それっぽい値段」を自動算出する点がコンセプトの核（同じテキストなら再現性のある値段になりつつ、「値段を引き直す」ボタンでシャッフルし直せる）。一定確率で「本日の特売」演出（取り消し線＋値引き後価格）も混じる。店名は自動生成/編集可、合計欄ラベルは既存アプリと別文言セットでシャッフル、末尾にバーコード風装飾を配置し、最終的に1枚のPNGとしてCanvas 2Dで書き出せる。バックエンドなし、完全クライアントサイド完結。

スタイル方向はダークラグジュアリー（既存のreceipt-poem-generatorのエディトリアル方向とは意図的にトーンを変えている）。漆黒〜チャコールのギャラリー展示ケース風背景の中央に、スポットライトを浴びた生成り色の感熱紙ロールを浮かべ、シャンパンゴールドを「値段を引き直す」ボタン・バーコード・合計欄罫線・フォーカスリングなど注目させたい要素だけに限定使用する。タイポグラフィは店名見出しにZen Old Mincho/Cormorantのセリフ、レシート本体にDotGothic16のドットマトリクスを対比させ、「高級店の値札」と「機械印字」のギャップを演出する。技術スタックはVite + TypeScript（バニラ、状態管理ライブラリなし）+ Canvas 2D。

Design QA（2体・2ラウンドとも合格）: 階層・余白リズム・奥行き（回転transform+多層shadow+feed-outアニメーション、prefers-reduced-motion対応）・タイポグラフィ対比・意味を持つ配色・hover/focus/active状態など必要品質を満たしていると判定。Verify（1ラウンド目は不合格→2ラウンド目で合格）: npm build成功、Playwrightでの実操作（サンプル投入→値段引き直し→店名生成→PNG書き出し）・375/768/1440幅のレイアウト確認とも問題なし、アプリ内JSエラー0件。**Dockerビルドのみ未検証**: このセッションのegressポリシーがdocker.io（node:22-alpine / nginx:alpineの取得）を403 Forbiddenで一律拒否するため、prototype-builder・prototype-verifier・メインセッションいずれも独立に`docker build`を試行したが再現し、Dockerfile自体の構文・COPYパスに欠陥は見当たらなかった（image/containerは一切作成されず、クリーンアップ対象なし）。

本パイプラインの実行は、2026-08-25〜26のバッチ検証で発生した「ログファイルリネームがClaude Codeのsensitive file保護に引っかかりrequires_actionで停止し、コミット前に失われた」個体のサルベージ再生成（詳細は`.claude/CLAUDE.md`「活動ログ」節を参照）。

**採否待ち**: 機能・デザインはメインセッションが独立に動作確認済み。Dockerビルドのみ環境要因（本サンドボックスのegress制約）で未検証。採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する（本実装移行時にDockerビルドも改めて確認すること）。

### アンビエント映画字幕オーバーレイ（`apps/ambient-subtitle-cam/`）

app-factoryパイプラインが自律生成したプロトタイプ。自室のウェブカメラ映像に、音量スパイクと動き検出（画素差分）だけをトリガーにした映画風フェイク字幕（`[遠くでサイレンが鳴る]`等）をリアルタイムで重ねるネタツール。物体認識・音声認識等のAI/ML処理は一切行わず、音量RMSと画素差分の閾値判定のみで駆動する軽量な仕組み。ムード3種（サスペンス／メロドラマ／日常コメディ）×トリガー3種、計99件のキャプションバンクを持ち、感度スライダー・手動キュー（キーボードショートカット）・PNGスナップショット・3秒WebMクリップ保存に対応。

スタイル方向はダークラグジュアリー×フィルムノワール／アートハウス上映室調（マットブラック基調にシャンパンゴールド/真鍮の差し色、レターボックス黒帯・フィルムグレイン・ビネット・インクリメントするモノスペースタイムコード・点滅REC・白文字+黒縁取りの映画字幕）。技術スタックは素のHTML/CSS/JS（Canvas API + getUserMedia + Web Audio API + MediaRecorder API、ビルドはVite利用のみでフレームワーク不使用）。

Design QA（2体・ラウンド1で合格）: 階層・余白リズム・奥行き・タイポグラフィ対比・意味を持つ配色・hover/focus/active/disabled状態のいずれも満たしていると判定。Verify: `npm run build`成功、ヘッドレスChromiumでのカメラ/マイクモック実機確認（起動→自動トリガー発火→手動キュー→ムード切替→PNGスナップショット→WebMクリップ保存の一連のフロー動作、375/768/1440幅でレイアウト崩れなし、コンソールエラーはfavicon 404のみ）、`docker build`+起動でHTTP 200を確認（検証後image/container削除済み）。

夜間の無人パイプライン実行がブランチをpushしたままmainへの反映を伴わず、レビュー待ちのまま4日以上放置されていたのをメインセッションが発見。独立worktreeで`npm run build`・`docker build`+起動+HTTP 200・Anti-Template Policy（ソースコード読み込みベース）を再確認し、いずれも合格したためmainにマージした（2026-08-28）。

**採否待ち**: ビルド・Dockerビルド・Anti-Template Policyはメインセッションが独立に確認済み。ブラウザでの実機確認（カメラ/マイク権限フロー、実際の録画・スナップショット動作）はまだ行っていない。本実装として採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する。

### 締切フライト案内板（`apps/deadline-departure-board/`）

app-factoryパイプラインが自律生成したプロトタイプ。複数の締切・予定日（税金申告、納品、契約更新など）をローカルに登録すると、空港・駅の発着案内板のようなスプリットフラップ（パタパタ）アニメーションで残り日数・時刻をめくって表示するボード。締切が近いものは自動的に上位へソートされ、SCHEDULED/BOARDING/FINAL CALL/DEPARTEDの4段階でステータスが遷移し、しきい値を跨ぐ瞬間にフラップがめくれる。DEPARTED後は一定時間表示してから自動的に消える。

スタイル方向はレトロフューチャリズム（1970〜80年代のSolari式空港発着案内板の再現）。近黒〜チャコールの筐体にオフホワイト文字のフラップタイル、コンデンスド・グロテスク書体（Oswald）、ステータス・ライブ時計にアンバー/ゴールドの差し色、スキャンライン/グレインのオーバーレイでヴィンテージ感を演出。フラップの回転はCSSのrotateXアニメーションのみで実装し、値が変化した瞬間だけめくる（常時アニメーションはしない）。技術スタックは素のHTML/CSS/JS（npm依存なし、ビルド不要）、データ永続化はlocalStorageのみ。

Design QA（2体）・Verifyともにラウンド1で合格。Verifyではローカル起動・375/768/1440幅での表示崩れなし・コンソールエラー0件・Docker独立ビルド成功（確認後にimage/container削除済み）を確認。既知の軽微な点: 検証時のサンドボックス環境にCJKフォントが入っておらず日本語UI文言がスクリーンショット上は文字化けして見えたが、CSS側は日本語フォントへのフォールバックを明示済みでアプリ自体のバグではない。

夜間の無人パイプライン実行がブランチをpushしたままmainへの反映を伴わず、レビュー待ちのまま4日以上放置されていたのをメインセッションが発見。独立worktreeで`npm run build`・`docker build`+起動+HTTP 200・Anti-Template Policyを再確認し、いずれも合格したためmainにマージした（2026-08-28）。

**採否待ち**: 採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する。

### タブ庭園（Tab Guilt Garden）（`apps/tab-guilt-garden/`）

app-factoryパイプラインが自律生成したプロトタイプ。このアプリを開いた各ブラウザタブごとに苗が1本生え、複数タブを開くとその全部が「庭」として1画面に集約表示される。しばらくフォーカスしていないタブの苗は現実の経過時間に応じて発芽→若葉→満開→しおれ→枯死としおれていき（内部の崩壊は分単位で高速チューニングしつつ、表示テキストはx60倍で「3時間放置」のように誇張して見せる）、閉じたタブは記念に墓標を残す。タブを溜め込みがちな自分への自虐ジョークツール。crypto.randomUUID()でタブごとにIDを発行し、BroadcastChannel+localStorageのみでタブ間状態を同期する（バックエンド・サーバー不要）。ハートビート途絶によるゴースト判定（クラッシュしたタブが庭に残り続けないようにする仕組み）も実装済み。

スタイル方向はネオブルータリズム（自虐ジョークツールの語調に合わせ、上品にまとめず罪悪感を煽る荒々しさを選択）。太い黒枠、からし色/警告レッド/庭グリーンのベタ塗り3色構成＋生成り紙色ベース、貼り付けたような紙工作風のオフセットドロップシャドウ。Archivo Black見出し＋Space Grotesk本文＋JetBrains Monoデータ表示の3書体使い分け。技術スタックはVite + TypeScript（バニラ）。

Design QA（2体・1ラウンドで合格）: 階層・余白リズム・奥行き（オフセット影・YOUバッジの重なり・カード個別回転）・タイポグラフィ対比・意味を持つ配色（緑=生存/赤=墓標・危険/マスタード=行動導線）・hover/focus/active状態の作り込み、必要品質6項目すべて満たしていると判定。Verify（機能・レイアウト）: npm build成功、Playwrightで複数タブ間同期（2タブ間での庭の状態反映）・名前編集の即時反映・タブクローズ時の墓標生成・カスタム確認モーダルによるリセットの一連動作を確認、375/768/1440幅でレイアウト崩れなし、アプリ内JSエラー0件。

夜間の無人パイプライン実行がブランチをpushしたままmainへの反映を伴わず、レビュー待ちのまま放置されていたのをメインセッションが発見。独立worktreeで`npm run build`・`docker build`+起動+HTTP 200・Anti-Template Policyを再確認したところ、いずれも合格（**当時未検証だったDockerビルドも、今回の環境では問題なく成功・200応答を確認**。旧記録の「egress制約で未検証」はこのセッションの環境条件下の話であり、コード側の欠陥ではなかったことが裏付けられた）。これによりmainにマージした（2026-08-28）。

**採否待ち**: 機能・デザイン・Dockerビルドとも動作確認済み。採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する。

### クラドニ図形ポスタージェネレーター（Chladni Cymatics Poster Lab）（`apps/chladni-poster-lab/`）

app-factoryパイプラインが自律生成したプロトタイプ（夜間無人実行）。振動モードn・m（整数スライダー、1〜9）を指定すると、標準Chladni式 z=cos(nπx)cos(mπy)−cos(mπx)cos(nπy) から算出した節線パターンに2000〜5000個の砂粒子がCanvas上でリアルタイムに収束していく物理シミュレーションを描画し、その瞬間をモード番号・周波数・プレート寸法入りの「実験記録」ポスターとしてPNG（高解像度合成）/SVG（マーチングスクエア法で節線をベクトルパス抽出、自前実装）で書き出せるツール。プレート形状は正方形/円形（円形は正方形変位場を境界クリップする近似実装、README・コンセプトに明記）。バックエンドなし、完全クライアントサイド完結。

スタイル方向はレトロフューチャリズム。操作パネルはガンメタル/ブラッシュドアルミ調のダーク背景+CRTフォスファーグリーン(#39ff6a)アクセント+ヴィンテージ・スライドフェーダー風スライダー+LED風モノスペース数値表示(JetBrains Mono)+スキャンライン/ビス(ネジ)ディテール、見出しはSpace Grotesk。一方、書き出されるポスター本体は生成り紙(#f3efe3)+インク(#1c1a16)+ヴァーミリオン差し色(#c1442a)の技術図面風レイアウトで、「操作パネル=レトロ機材」「出力物=ラボ実験記録」と意匠を明確に切り分けている。技術スタックはVite + TypeScript、外部シミュレーション/描画ライブラリ不使用（Canvas 2D API直描画）。

prototype-builderがheadlessブラウザでの独立動作検証中に実バグを3件発見・修正済み: (1) `#simCanvas`が`position: absolute`でなかったためResizeObserverフィードバックループが発生し、キャンバスが際限なく肥大化して粒子が画面外に描画される不具合、(2) 粒子への力に勾配の生の大きさをそのまま使っていたため速度が発散し節線に収束しない不具合（勾配の向きのみ正規化し|z|に応じてステップ幅を縮小する方式に修正）、(3) PNG書き出しの完了報告が`canvas.toBlob`の非同期完了を待たずに発火していた不整合（Promiseを正しくawaitするよう修正）。

Design QA（2体・2ラウンドとも合格）: Anti-Template Policyの階層・余白リズム・奥行き（ボルト装飾・スキャンライン・inset shadow・phosphorグロー・パルスアニメーション）・タイポグラフィ対比・意味を持つ配色（緑=稼働中/琥珀=再試行/赤=エラー、ポスター側は象牙紙+朱色で別の意味体系）・hover/focus/active/disabled状態のいずれも満たしていると判定。Verify: `npm run build`成功、Playwright実機操作（n/mモードスライダー・形状トグル・砂粒子密度・ランダムリトライ・PNG/SVG書き出し）で375/768/1440幅ともレイアウト崩れ・コンソールエラーなし、`docker build`（node:20-alpine→nginx:alpine多段構成）を独立に確認し200応答後にimage/container削除済み。

**このエントリは`.github/workflows/app-factory-nightly.yml`による無人夜間実行が生成したもので、CLAUDE.mdの方針に従いmainには直接マージせず、`app-factory/auto-chladni-poster-lab`ブランチをpushしてmain宛てPRを作成した。** メインセッションによるレビュー（ビルド・Dockerビルド・Anti-Template Policy・実機確認）を経てマージ済み。

**採否待ち**: 機能・デザイン・Dockerビルドとも動作確認済み。採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する。

### オーロラ・テルミン（Aurora Theremin）（`apps/aurora-theremin/`）

app-factoryパイプラインが自律生成したプロトタイプ。マウス/指の位置と移動速度でWeb Audioのテルミン風シンセを演奏する楽器トイ。ホールド+ドラッグ中のみ発音し、X座標→ピッチ（対数マッピング・2オクターブ・量子化なしの連続グリッサンド）、Y座標→音量、移動速度→ビブラート深さ/フィルターカットオフにマッピングする。奏でた音に呼応してCanvas上の自作value noiseによるオーロラ状グラデーションが揺らめき（無操作時も控えめなアンビエントドリフトを継続）、ジェスチャーを最大45秒録音して自動ループ再生、最大3レイヤーまでの重ね録り（オーバーダブ、個別ミュート/クリア可）ができる。マイク/カメラ等の実センサー入力は使わず、既存のマイク/カメラ系ジョークアプリとは差別化している。バックエンドなし、完全クライアントサイド完結。

スタイル方向はレトロフューチャリズム（アナログシンセサイザー/宇宙船コンソール意匠）。ダークネイビー（#0a0e14〜#12161f）の筐体にブラッシュドメタル風の微弱グラデーション、操作系のアクセントはアンバー（#ffb454）とシアン（#4dd8e0）のLED/CRT発光色に絞る。Hz/dBのデジタル数値リードアウトにJetBrains Mono、ラベルはOswaldの全大文字広字間で計器の銘板調に。Canvasを画面いっぱいの「窓」とし、下部に録音トグル・レイヤーLED3灯・クリアスイッチ・数値リードアウトを収めた固定コンソールパネルを額縁として重ねる。LEDは待機/演奏中/録音中/ループ再生中で発光色・点滅パターンを変え、状態フィードバック自体を意匠の一部にしている。技術スタックはVite + TypeScript（フレームワーク非依存、Web Audio APIとCanvas 2Dを直接扱い、ノイズ関数も外部ライブラリなしで自前実装）。

Design QA（2体・ラウンド1で合格）: 階層（フルビューポートのCanvasと小型高密度なコンソールパネルの対比）・奥行き（多層box-shadow、ブラッシュドメタルのテクスチャ疑似要素、CRTスキャンライン/ヴィネット、REC/RESETの立体表現）・タイポグラフィ対比（Oswald×JetBrains Mono）・意味を持つ配色（amber/cyan/redが機能状態に対応、Canvas自体もピッチで色相変化）・作り込まれたhover/focus/active状態のいずれも満たしていると判定。Verify（ラウンド1で合格）: `npm run build`成功、375/768/1440幅でコンソールエラー0件・レイアウト崩れなし。実機操作（ドラッグでの演奏・録音トグル→自動ループ→レイヤーLED点灯）を確認。実装中にPlaywrightでの自動操作検証によりヒントオーバーレイのpointer-events不備（初回ドラッグが演奏に届かない）、録音停止時のレイヤーUI更新順序バグの2件を発見・修正済み。**Dockerビルドも独立確認済み**（`docker build`〜`docker run`〜HTTP 200応答、確認後にimage/containerとも削除）。

無人の夜間パイプライン実行（GitHub Actions）のため、`.claude/CLAUDE.md`の運用方針に従いmainには直接マージせず、`app-factory/auto-aurora-theremin`ブランチにpushしPull Requestを作成した。メインセッションによるレビュー（ビルド・Dockerビルド・Anti-Template Policy・実機確認）を経てマージ済み。

**採否待ち**: 機能・デザイン・Dockerビルドまで一通り動作確認済み。採用するかどうかはユーザー判断待ち。採用する場合は`.claude/CLAUDE.md`の「プロトタイプ採用後の引き継ぎ」に従ってsdd-managerに本実装を依頼する。

## アイディア

### 請求書・見積書ジェネレーター（フリーランス向け）

項目を入力すると即座にプレビューが表示され、そのままPDFとしてダウンロードできるツール。フリーランスが業務で継続的に使う実用ツールとして、外部にも見せやすい。差別化ポイントはテンプレートデザインの質（既存の無料ツールはデザインが古い/事務的なものが多い）。

### 画像から配色抽出＋パレット書き出し

画像をドロップすると主要カラーを抽出し、Tailwind設定やCSS変数の形式で書き出せるツール。デザイナー・コーダー向けの実用ツールで、ビジュアル要素が強くポートフォリオとして見せやすい。

### 案件・要員マッチングツール（SES営業向け）

自社の営業（SES系）がメールなどで受け取る案件概要と、社内の要員情報（スキルシート等）を突き合わせて、マッチしそうな組み合わせを見つけ出すツール。案件文面と要員情報はどちらも自由記述・非定型なことが多いため、単純なキーワード一致ではなくスキル・条件の意味的なマッチングが鍵になりそう。営業が日常的に行っている「案件と人をつなぐ」作業の支援を想定。要件・技術構成は未検討（次の検討フェーズで詰める）。

### イメージ重視のLP（「想像力を翼に」）

ビジュアル表現そのものを主役にしたランディングページ。トップのキャッチコピーは「想像力を翼に」。何を訴求する対象のLPかは未定で、まずはコピーと世界観から発想している段階。スタイル方向・配色・タイポグラフィなどの具体化はこれから。

### ラテン語を勉強できるサイト

ラテン語の学習サイト。対象レベル・学習範囲（文法／語彙／読解など）・教材形式（クイズ形式か読み物形式か等）はまだ未検討。

### CSSコードスニペット閲覧サイト

CSSのコードスニペットを集めて閲覧できるサイト。掲載するスニペットの傾向（アニメーション、レイアウト、装飾など）や、他人の投稿を集めるのか自作のみか、閲覧以外の機能（コピー、検索、カテゴリ分けなど）を持たせるかは未検討。

## 完成

### スクショ継ぎ足し（Shot Splice）（`apps/shot-splice/`）

3スタイル比較プロトタイプ（下記「プロトタイプ」節参照）はどれもデザインとして採用せず、「アルゴリズムのみ引き継ぎ、デザインはiOSライク・モバイルファーストで新規に作り直す」方針でsdd-managerに本実装を依頼。2枚に分けて撮ったスクリーンショットの重なりを自動検出し1枚に継ぎ足す元々の機能に加え、以下を新規実装した:
- モバイルファースト（900px未満は縦積み1カラム、900px以上でステージ+サイドバーの2カラムグリッドへ。基本設計がモバイル優先で、デスクトップは追加のメディアクエリで拡張する構造）
- iOSライクなデザイン（`-apple-system, BlinkMacSystemFont`のシステムフォントスタックでApple端末では実際にSan Franciscoフォントが表示される。`env(safe-area-inset-*)`でnotch/ホームインジケータに対応）
- 3枚以上（任意枚数）の画像合成への一般化
- 全ショットに共通する固定ヘッダー/フッター帯の自動検出・提案（無言で不可逆にカットせず、検出結果をカードで提示してユーザーが確認・調整できる）

speckitは本環境に未インストールのため、SDD成果物（`docs/SPEC.md`/`PLAN.md`/`TASKS.md`）は既存の慣例に合わせて手書きで作成。22タスク・22コミットで実装。

**実装中にsdd-manager自身が発見・修正した不具合6件**: 手動調整後も直前の自動検出時の一致度表示が残り続けてUIが実態と食い違っていた問題、ヘアラインのコントラスト不足（実測1.17→1.4超）、タイトルの階層不足（390px幅で25.9px→32.6px）、stickyステージ上端の段差（背景マスクの当て方を修正）、並べ替え往復で計測値が消失する不具合、棄却候補の一致度が無関係な数値の隣に表示される不具合。いずれもコードレビューや単一スクリーンショットでは見つからず、実際の操作・実測で発覚したもの。

**独立検証結果（メインセッション実施）**: `node_modules`/`dist`/`coverage`を削除してからの`npm install`→`npm run build`→単体テスト実行まで独立に再現し、221件全緑（sdd-manager報告の値と一致）。`docker build`〜`docker run`〜HTTP 200応答を独立に確認し、確認後はimage/containerとも削除済み。コードレベルでモバイルファースト構造（`@media (min-width: 900px)`のみ、ベースは縦積み）・`env(safe-area-inset-*)`の実使用・システムフォントスタックを確認。ブラウザでの狭幅（390px相当）実機確認は、本セッションのブラウザ操作ツールでウィンドウリサイズがビューポートに反映されない制約（[[tab-guilt-garden]]のエントリでも既知の制約として記録済み）により再現できておらず、sdd-manager報告のPlaywrightによる320〜1280幅・3ブラウザでの検証結果は自己申告のまま未再実行。

**ユーザー確認・mainマージ済み**（2026-09-01）。実機（スマートフォン、`--host`でのLAN内アクセス）で確認したフィードバック: 「だいぶ良くなってるが、スマホライクなアプリとしてはもう一歩」。具体的な指摘: (1) **UI/UXの段階で「スマホならこういう動線/設計にする」という検討が薄い**——スクロールすると画像プレビューが見えなくなり、操作（重なり調整等）と確認を同時に行えない。(2) 背景のノイズ質感がスマホアプリっぽくない。ユーザー自身の言葉で「(2)より、その前の広い意味でのデザイン（情報設計・操作導線）が弱いことが本質」とのこと。レスポンシブCSS対応・システムフォント採用等の技術要件を満たすことと、体験としての「スマホアプリらしさ」は別物だと分かった実例（教訓を`mobile-first-ux-vs-visual-polish`として記憶に保存済み）。次にこのアプリに着手する際は、この操作導線の課題を軸にUI再設計を検討すること。3スタイル比較プロトタイプ（`shot-splice-darkroom`/`shot-splice-grid`/`shot-splice-brutal`）を残すか削除するかは未定のまま。

### タブ庭園（Tab Guilt Garden）（`apps/tab-guilt-garden/`）

app-factoryパイプラインが自律生成したプロトタイプが起点。ユーザーが採用し、sdd-managerが本実装まで実施（[Pagesプレビュー](https://oplan-development-team.github.io/web-app-factory/tab-guilt-garden/)）。このアプリを開いた各ブラウザタブごとに苗が1本生え、複数タブを開くとその全部が「庭」として1画面に集約表示される。しばらくフォーカスしていないタブの苗は現実の経過時間に応じて発芽→若葉→満開→しおれ→枯死→立ち枯れ→化石化としおれていき（内部の崩壊は分単位で高速チューニングしつつ、表示テキストはx60倍で誇張して見せる）、閉じたタブは記念に墓標を残す。タブを溜め込みがちな自分への自虐ジョークツール。`crypto.randomUUID()`でタブごとにIDを発行し、`BroadcastChannel` + `localStorage`のみでタブ間状態を同期（バックエンドなし）。スタイル方向はネオブルータリズム。技術スタックはVite + TypeScript（バニラ）。

**採用時のフィードバックへの対応**: 「放置系インクリメンタルゲームでもある」という指摘を受け、焼き払っても消えない通算記録（Lifetime Ledger）・階級ラダー・実績10種・枯死の先の「立ち枯れ→化石化」を新設し、放置ゲームとしての進行感を強化した。また「アプリ単体で何をするアプリか伝わりにくい」という指摘に対し、初回訪問時の3ステップ説明パネル（「これは何？」ボタンで再表示可）とヘッダーのリード文を追加した。

**プロトタイプからの実装判断**: `style.css`のデザイントークン・純粋関数・localStorage/BroadcastChannel構成はそのまま土台に採用。`main.ts`のみ作り直し（import時副作用でテスト不能だったため）。プロトタイプの実装を読解する過程で、ゴースト埋葬の自己ブロードキャスト・墓標の重複排除漏れ・localStorage容量超過未処理・bfcache復帰時の状態不整合・機能検出漏れの5件の不具合を発見し修正した。

**独立検証結果（メインセッション実施）**: `npm ci`→`npm run test`成功（308件全緑、行カバレッジ90.58%、閾値80%）、`npm run build`成功、`npm audit`0件。`docker build --no-cache`〜起動〜HTTP 200を独立に再現し、確認後はimage/containerとも削除済み。ブラウザ実機で3タブを開いて同期・intro説明パネルの表示/再表示・タブを閉じた際の即時墓標生成・実績解除トーストを確認、コンソールエラー0件。sdd-manager報告のPlaywrightによるレスポンシブ検証（320/375/768/1440幅）は、本セッションのブラウザ操作ツールでウィンドウリサイズがビューポートに反映されない制約があり再現できていない（自己申告のまま未再実行）。

既知の制約: `main.ts`（139行、配線のみ）はimport時副作用のためテストカバレッジ0%（意図的なトレードオフ、全体は90.58%）。`style.css`は800行上限のため6つのpartialに分割済み。

ユーザーがChatGPT Imageで生成した完成イメージを起点に、sdd-managerが仕様作成〜実装〜検証まで実施。mainにマージ済み（[Pagesプレビュー](https://oplan-development-team.github.io/web-app-factory/column-daily/)）。ヴィンテージ新聞・エディトリアルスタイルのコラムメディアサイト。ブラックレター（UnifrakturCook）のロゴタイトル「The Column Daily」＋Shippori Minchoの和文本文。トップページ（メイン特集＋新着コラム4件＋サイドバー）、記事詳細、カテゴリー別一覧、新着/人気/連載一覧、検索、コラム投稿ページ、ログイン/会員登録/マイページのダミー画面を実装。バックエンドなし、完全クライアントサイド完結。技術スタックはVite + React + TypeScript + react-router-dom（HashRouter、GitHub Pagesにhistory fallbackが無いため採用）。

**要件上の重要な設計判断**: 「コラムを書く」フォームは送信しても実際には保存されない（画面・フッターに明記）。ログイン・会員登録には認証情報の入力欄自体を用意していない（バックエンドの無い画面で実在のパスワードを入力させる誘導を避けるため、判断理由も画面上に明記）。これは着手前にユーザーと擦り合わせて確定した要件（永続化なし・認証ロジックなし）をそのまま反映したもの。

**初回実装への差し戻しとメインセッションによる再制作（2026-08-28）**: sdd-manager初版は「参考画像との意匠の一致度も高い」とメインセッションが評価してユーザーに報告したが、ユーザーから「クオリティが足りない。まさしく同じ見た目に作り直してほしい」と明確な差し戻しを受けた。参考画像をピクセル単位で再検分したところ、複数の実装上の乖離を発見（メインセッションの初回確認が甘かった）:
- ページ全体が濃茶色の台紙（デスク）に載った角丸カードの紙面という構成を再現していなかった（フルブリードの紙質感のみだった）
- ナビゲーションは黒帯+白文字ではなく、紙色の帯+黒文字で、現在地と「マイページ」だけが黒いピル型ボタンだった（マイページは朱色ではない）
- ランキング数字・カテゴリータグは朱色ではなく墨色系だった
- 記事画像は実写風の写真で、SVGの抽象イラストではなかった

メインセッションが直接（sdd-manager再発注ではなく）、参考画像の複数箇所をPythonでピクセルサンプリングして正確な配色・レイアウトを特定した上で修正した。記事画像はPicsum Photos（Unsplash由来、Unsplash License準拠・商用利用可）から記事の雰囲気に合わせて1枚ずつ選び、`src/assets/photos/`に静的バンドル（ユーザーに事前確認の上でダウンロード）。決定的インラインSVGイラストは廃止。

**独立検証結果（メインセッション実施、再制作後）**: `npm ci`＋`npm run build`成功（JS gzip 82.6KB、CSS gzip 7.2KB。予算150KB/30KB以内）。`docker build --no-cache`〜起動〜HTTP 200を独立に再現し、確認後はimage/containerとも削除済み。ブラウザ実機でトップ・記事詳細・投稿ページ・ログイン画面・カテゴリードロップダウンを確認、コンソールエラー0件。sdd-manager報告およびメインセッション初回確認時のPlaywright/ブラウザリサイズによるレスポンシブ検証は、本セッションのブラウザ操作ツールでウィンドウリサイズがビューポートに反映されない制約が続いており、再現できていない（未検証のまま）。

既知の制約: `react-router-dom` 7系・vite 8系で構成しており、他アプリ（多くはvite 5系）とスタックのバージョンが揃っていない。記事画像は`loading="lazy"`を意図的に使わず全数即時読み込みにしている（SPA内遷移でlazy属性の画像が読み込まれない再現性の高い不具合を確認したため。記事数が少ない本アプリでは実害が小さいと判断）。

### サイアノタイプ・ポスターメーカー（`apps/cyanotype-poster-maker/`）

app-factoryパイプラインが自律生成したプロトタイプが起点。sdd-managerによる本実装が完了し、mainにマージ済み。アップロードした写真、または本アプリが手続き生成する植物標本図案を、Floyd-Steinberg誤差拡散でプルシアンブルーの二階調ハーフトーンに変換し、紙の繊維テクスチャ・藍液の塗りムラ・周辺減光・不規則な感光縁（直線／ラフな手塗りの2種）まで再現して、植物標本ラベル（学名・和名・採集地・緯度経度・採集日・自動採番の標本番号）付きの一枚のサイアノタイプ風ポスターとして高解像度PNG書き出しできるツール。画像処理・図案生成はCanvas 2D APIによる自前実装のみで、外部ライブラリ・サーバー不要。

スタイル方向はエディトリアル（自然史博物館の植物標本アーカイブ／19世紀ハーバリウム様式）。ポスター本体はプルシアンブルー系インク色×生成りクリーム紙色の2色構成を厳守し、囲むツールUIはダークな陳列室の壁色＋クリームの索引カード風パネル＋真鍮アクセントで標本キャビネットの質感を出す。学名イタリック（EB Garamond）／日付・座標・標本番号のタイプライター風モノスペース（Special Elite）／産地のスモールキャップス本文セリフという3層タイポグラフィ対比が特徴。技術スタックはVite + TypeScript（バニラ）。

**採用時のフィードバックへの対応**: 「画像アップロードのみ」だったプロトタイプに、良い被写体が手元になくても一枚目に到達できるよう「所蔵標本（プリセット図案）」を追加。シダ・海藻・葉脈標本（インドボダイジュ）・イチョウ・エノコログサ・ノラニンジンの6種を、シードから決定的に組み立てる図形として実装（静的画像でないため高解像度書き出しでも拡大ボケが出ない）。階調は陰画（フォトグラム）方式（植物が光を遮って紙色に残り、地が藍に沈む）で、Anna Atkinsの原典的な焼き付け方に合わせている。図案帳サムネイルは本番と同じ生成器・誤差拡散で描画し、選ぶ前に結果が分かるようにした。標本選択時のラベル自動投入は、ユーザーが手で編集した項目を上書きしない設計。

本実装ではあわせて、tsconfigの`strict`+`noUncheckedIndexedAccess`化、描画層を`CanvasRenderingContext2D`直依存から構造的部分型`Ctx2D`へ切り出し（jsdomにCanvas実装が無く、これがないと描画のテストができないため）、状態管理の不変リデューサ化、感光しきい値スライダーが実際には効いていなかった不具合の修正（誤差拡散の閾値でなく露光量として作用するよう修正）などの堅牢化を行った。

**独立検証結果（メインセッション実施）**: `npm run build`成功／単体・統合テスト316件成功（v8カバレッジ: 行99.44%・関数98.36%・分岐82.07%、しきい値80%通過）／`docker build --no-cache`〜コンテナ起動〜HTTP 200応答を独立に再現、確認後はimage/containerとも削除済み／ブラウザで所蔵標本の選択→感光しきい値調整（反映を確認）→高解像度PNG書き出しの一連のフローを実機確認、コンソールエラー0件。sdd-manager報告のE2E（Playwright 14件×3エンジン=42件成功）は自己申告のまま未再実行。

既知の制約: プレビュー再描画・サムネイル描画のパフォーマンス目標（NFR-003.1/003.2）は数値未計測（E2Eは通過しているが専用ベンチなし）。VitestとPlaywrightの同時実行はCPU競合でテストが不安定になるため、直列実行が必須（README・TASKS.mdに注意書きあり）。

### オリジナル家紋ジェネレーター「家紋帳」（`apps/kamon-generator/`）

sdd-managerによる本実装が完了し、mainにマージ済み（[Pagesプレビュー](https://oplan-development-team.github.io/web-app-factory/kamon-generator/)）。名前や誕生日などの文字列をシードに、正円グリッド上で左右対称・点対称の紋様を決定的に自動生成する、日本の伝統的な家紋風のジェネレーター。「家紋帳」の見開きページを開く体験として画面を構成し、和紙のオフホワイト背景に墨色、朱色を唯一の差し色とするエディトリアルなスタイル。

**採用時のフィードバックへの対応**: 「小さな部品を円の中に散らす」設計から「少数の大きな面を中心で噛み合わせて一つの塊にする」設計に転換。線幅は面（塗り）主体に、要素数は最大8個（実測1〜6）に、紋様域の充填率は85〜95%に収まるよう不変条件化し、100シード×3バリアントの全数走査で回帰検証している。メインセッションでも実機（ブラウザ）で複数パターンを生成し、太い塗り主体・少要素数の家紋らしい構図になっていることを確認済み。

**独立検証結果（メインセッション実施）**: ビルド成功（JS gzip 10.89KB）／単体・統合テスト174件成功（行カバレッジ97.1%）／ブラウザ実操作でコンソールエラーなし／Dockerビルド・起動を独立に再現し200応答を確認、確認後はimage/containerとも削除済み。マージ後、mainの状態でも改めてビルド成功を確認済み。sdd-manager報告のE2E（Chromium/Firefox/WebKit 62件成功）は自己申告のまま未再実行。

既知の制約: 構成の出現比率に偏りあり（`crossed`は鷹の羽のみ、`ring`は菱・目結のみ対応のため構造的に少数）。割り出し表示に最短260msの意図的な待ちあり（`prefers-reduced-motion`では即時）。

**発展版に着手**: ユーザー画像アップロード・投稿・reactionを含むコミュニティサイトへ拡張する案は、継続運用・複数ユーザー管理・継続インフラ費用が前提のため中規模以上と判断し、別リポジトリ `../kamon-composer/`（Vite+React / Supabase）で着手（2026-08-26）。外郭・構成・座標計算ロジックは移植済み。本アプリ（`apps/kamon-generator/`）自体はこのまま完成・独立稼働のアプリとして残る。

### 署名のリボン光跡（Signature Ribbon Poster）（`apps/signature-ribbon-poster/`）

sdd-managerによる本実装が完了し、mainにマージ済み（[Pagesプレビュー](https://oplan-development-team.github.io/web-app-factory/signature-ribbon-poster/)）。マウス/タッチ/ペンで描いた署名・一筆書きのストロークを、速度に応じて太さと発光の強さが変わる「リボン」として暗い背景に描画し、任意のキャプション（署名者名・日付）付きの一枚のポスターとして高解像度PNGで書き出せるツール。ダークラグジュアリー・スタイル。技術スタックはVite + TypeScript（Canvas 2D API直描画）。

**採用時のフィードバックへの対応**: shadowBlur3パス→多段ブルーム（縮小・ぼかし・加算合成）へ置換、ドラッグ中は確定済みセグメントを保持し新規分のみ増分描画、表示解像度を`表示サイズ×min(dpr,2)`に適正化（塗り面積が約1/12）。sdd-manager計測では1000点描画時、旧実装は中央値6msだが5回に1回3.5秒のストールが発生（GPU無効環境では40〜50秒）、新実装ではストール自体が発生しない。追加機能: 書き出し解像度3段（Study/Edition/Archival）、下書き自動保存・復元、Redo、Response（描画の反応特性）スライダー。ズーム/パンは「速度の連続性が体験の核」との理由で意図的に不採用（SPEC記載）。

**独立検証結果（メインセッション実施）**: ビルド成功（JS gzip 9.52KB）／単体・統合テスト325件成功（カバレッジ98.04%）／ブラウザで実際にドラッグ描画しコンソールエラーなし・発光色も脱色していないことを確認／Dockerビルド・起動を独立に再現し200応答を確認、確認後はimage/containerとも削除済み。マージ後、mainの状態でも改めてビルド成功を確認済み。sdd-manager報告のE2E（Playwright 15シナリオ成功）とベンチマーク数値は自己申告のまま未再実行。sdd-manager自身の注記: 自動化されたマウス操作は速度が不均一なため光跡が数珠状に見えることがあり、実際の手描きでの確認が確実とのこと。

既知の制約: `ctx.filter`非対応環境（Safari 17未満）ではブルームがやや硬くなる。Archival書き出しは端末により数秒かかる。`localStorage`不可環境では下書き保存が働かない（通知は出る）。スマートフォンは最適化対象外。

### ハーフトーンQR（`apps/halftone-qr/`）

画像を網点に分解して、QRコードの模様そのものに溶け込ませるジェネレーター。姉妹アプリ`apps/qr-code-designer/`の「中央にロゴを載せる」方式とは異なり、画像全体をQRの模様に溶け込ませるのが差別化点。Chu et al.「Halftone QR Codes」（SIGGRAPH Asia 2013）の中核アイデアを実装し、QRの1モジュールを3×3のサブモジュールに分解、中央だけ元のビット値を必ず保持することで誤り訂正データを守りつつ、残り8個を誤差拡散法（Floyd–Steinberg）で画像の濃淡に合わせて塗る。

読み取り判定は静的推定ではなく、生成物をZXingで実際にデコードして行う（解像度3段階×ぼかし3段階の9条件、Web Worker上で実行）。技術スタックはVite + React + TypeScript。単体テスト111件通過、実ブラウザ検証済み。

前回セッションで完成していたが、ブランチがmainにマージされず`PROJECTS.md`への反映も漏れていたものを、今回発見してマージ・Docker対応・独立検証まで実施した。

既知の制約: 書き出したPNGを等倍のままソフトウェアデコーダに渡すと失敗することがある（ハーフトーンQRはカメラの光学的なぼけを前提とする方式のため。カメラで撮るか縮小してから読ませる）。カラー画像は未対応（グレースケールのハーフトーンのみ）。論文にある誤り訂正符号の再エンコード最適化は未実装。SVG/PDF書き出しは非対応（本質的にビットマップのため）。

### 生まれた瞬間の星空ポスタージェネレーター（`apps/birth-sky-poster/`）

日時・場所を入力すると、その瞬間その場所から見えていた星空を天文計算（外部API不要・自前実装）で再現し、計器図面のような精密な円形星図ポスターとしてPNG/SVG書き出しできるツール。既存の星空ポスターサービスが軒並み「ダークネイビー＋白ドット＋筆記体」の同一様式に収斂している中、あえて航海図・測量図のような精密さ（スイス・インターナショナルスタイル）で差別化している。

app-factoryパイプラインが自律生成したプロトタイプが起点。採用後、sdd-managerが仕様書化（`apps/birth-sky-poster/docs/`）・実装・検証まで実施した。プロトタイプ期間中のダミー恒星（乱数生成340個）は、コンセプトと矛盾するとして許容せず、d3-celestial同梱のHipparcos由来データに置き換え、実在恒星921個・IAU88星座に強化。天文計算・描画の中核で目視では気づけない不具合（極点での方位角退化、時刻の繰り上がり表示、年代の読み替え、「数値はモノスペース」という意匠がCSS詳細度の問題で一度も効いていなかった点など）を複数発見・修正している。単体テスト281件・カバレッジ98%台（閾値80%）、E2E110件（Chromium/Firefox/WebKit）通過。メインセッションの独立検証（ビルド・テスト再実行・Docker独立ビルド・ブラウザ実機確認）でも問題なし。

既知の制約: 歳差・固有運動・大気差の補正なし（仕様上許容、誤差は最輝星のドット半径以下）。恒星は4.5等級まで収録（6等まで収録すると密度過多で可読性を損なうため意図的に絞り込み）。太陽・月・惑星・深宇宙天体・複数カラーテーマ・都市名からの座標検索はスコープ外。

### QRコードデザインツール（`apps/qr-code-designer/`）

単なるQRコード生成ではなく、ロゴ埋め込み・ドット形状・グラデーションなどに対応したデザイン性の高いQR生成ツール。既存の無料QRツールの多くが画一的な黒白ドットのデザインである点を差別化ポイントとする。名刺・パンフレット・店舗POPなどでの実用を想定。

sdd-managerで仕様作成〜実装〜検証のフルサイクルを実施。技術スタックはVite + React + TS、QRの生モジュール取得のみ`qrcode-generator`に依存し、描画は手書きSVG（プレビューSVGがそのままエクスポート元）。単体テスト92件通過、ビルド後JS 78KB gzip。ブラウザでの手動確認でも主要機能（プリセット切替・グラデーション・ロゴアップロード・PNG/SVG書き出し）が動作し、コンソールエラーなしを確認済み。

既知の制約: スキャン耐性チェックはアプリ内で実デコードせず静的推定（印刷前に実機テストを促す表示あり）。反転配色のプリセット（ネオン）は反転リトライに対応しない読み取り機では読めない場合がある。E2Eテストは一時検証のみでリポジトリには常設していない（今後CI化する場合はPlaywrightの追加が必要）。

**要件**

- 入力: URLまたは任意のテキスト
- QRコード生成
  - エラー訂正レベルを選択可能（L / M / Q / H）
  - ロゴを埋め込む場合はエラー訂正レベルHを自動選択（またはユーザーに推奨を表示）
- デザインカスタマイズ
  - ドットの形状（角ばった四角 / 丸 / 角丸）
  - ファインダーパターン（3隅の目印）の形状・色を個別に変更可能
  - 前景色・背景色は単色またはグラデーションに対応
  - 中央にロゴ画像をアップロードして埋め込み（サイズ・余白を調整可能）
- プレビュー: 入力・設定変更をリアルタイムで反映
- 出力
  - PNG（印刷用途を想定し高解像度オプションあり）とSVGの両方に対応
- スキャン耐性への配慮
  - ロゴ埋め込みやデザイン変更でスキャン精度が落ちる場合に警告表示、またはエラー訂正レベルを自動調整
- データはすべてクライアントサイドで完結させ、サーバーへの送信を行わない（プライバシー面を訴求ポイントにする）

**技術メモ**: QRコード生成ライブラリ（qrcode.js系など）でベースのQRデータを生成し、Canvas/SVG上でドット形状・色・ロゴを重ねて描画する方式を想定。ロゴ埋め込み時は中央付近のモジュールをマスクしてロゴ画像を重ねる。

## 保留

### タイピング心電図（`apps/typing-ecg/`）

app-factoryパイプラインが自律生成したプロトタイプ。打鍵のkeydown間隔だけを計測し、1980〜90年代の病院用心電図モニター風UIでリアルタイムに波形を描画するネタツール。20打鍵以上で「速度×規則性」の6パターンのジョーク診断（安定パルス型・不整脈型など）を表示し、波形と診断結果を1枚のPNGに合成してダウンロードできる。入力文字は波形・診断に一切使わず、保存・送信もしない完全クライアントサイド完結。スタイル方向はレトロフューチャリズム（漆黒〜濃紺背景にフォスファーグリーンの波形トレース、アンバーの副次バイタル、走査線・CRTフリッカー）。Design QA・Verifyはいずれもラウンド1で合格。

**ユーザー評価（本実装は見送り）**: ビジュアルの完成度は高い（4.6/5点）が、コンセプトが浅い。「何をタイプすればいいか分からない」——お題や目的の提示がなく、単なる空欄にタイピングさせるだけなので、体験として「面白い」と感じるところまで届いていない。診断バリエーションも速度×規則性の2軸6パターンのみで少なく、すぐ飽きる。プライバシー重視の設計（入力内容は保存・送信しない）上、内容ベースの診断はできないため、改善するなら「具体的なお題を出す」「診断の軸・バリエーションを増やす」「診断結果の見せ方（処方箋・アドバイス調の一言）を作り込む」といった、タイミングデータだけで完結する範囲での深掘りが必要。次に着手するとしたら、ここが起点になる。

**重複生成の記録（2026-08-24、`app-factory/auto-typing-ekg`ブランチ、mainには未マージ）**: 夜間の無人パイプライン実行が、上記の見送り判断を認識しないまま「タイピング心電図」を独立に再生成した（slug: `typing-ekg`）。視覚方向（CRTオシロスコープ意匠・フォスファーグリーン配色）・波形描画ロジック（keydown間隔からのガウス関数合成）はtyping-ecgとほぼ同一系統で、独立した別アプリとは言い難いとメインセッションが判断し、mainへはマージしなかった。ただし入力方式は本アプリ（自由入力）と異なり5種のプリセット文をタイピングする方式で、上記評価にある「何をタイプすればいいか分からない」を部分的に是正している一方、診断機能自体は廃止しCPM/WPM等の定量サマリーに置き換えており「診断の楽しさ」は失っている。今後typing-ecgの改善に着手する場合、この差分（お題の提示）は参考になる。ブランチはレビュー用に残してあるが、削除しても実害はない。<br>**運用上の課題**: このケースは、夜間パイプラインが過去の「保留」判断を毎回参照していないために起きた重複生成の実例。idea-scout/idea-criticの重複チェックはPROJECTS.mdの各セクションを確認する運用だが、今回は素通りした。頻発するようなら、生成前のチェック手順を見直す必要がある。
