# PROJECTS

単発・小規模Webアプリのアイディアと進行状況の一覧。運用ルールは `.claude/CLAUDE.md` を参照。

## 進行中

（なし）

## アイディア

### 請求書・見積書ジェネレーター（フリーランス向け）

項目を入力すると即座にプレビューが表示され、そのままPDFとしてダウンロードできるツール。フリーランスが業務で継続的に使う実用ツールとして、外部にも見せやすい。差別化ポイントはテンプレートデザインの質（既存の無料ツールはデザインが古い/事務的なものが多い）。

### 画像から配色抽出＋パレット書き出し

画像をドロップすると主要カラーを抽出し、Tailwind設定やCSS変数の形式で書き出せるツール。デザイナー・コーダー向けの実用ツールで、ビジュアル要素が強くポートフォリオとして見せやすい。

## 完成

### ハーフトーンQRコードジェネレーター（`apps/halftone-qr/`）

画像をアップロードすると、その画像の見た目を保ったまま読み取り可能なQRコードに変換するツール。QRコードデザインツール（`apps/qr-code-designer/`）の「中央にロゴを載せる」方式とは異なり、**画像全体をQRコードの模様に溶け込ませる**のが特徴。既存のロゴ埋め込み方式より視覚的インパクトが大きく、ポートフォリオとしての見せ場になる想定。

参考にした研究・実装: Chu et al. "Halftone QR Codes"（SIGGRAPH Asia 2013）、実装例として [kloet.net/usr/halftone](https://kloet.net/usr/halftone/) を確認済み。

sdd-managerで仕様作成〜実装〜検証のフルサイクルを実施。技術スタックはVite + React + TS、QR生成は`qrcode-generator`（生モジュール行列のみ利用）、読み取り判定は`@zxing/library`をWorker内で動的importして実デコード、描画は素のCanvas 2D。単体テスト111件通過、初期バンドル77KB gzip（ZXingは別チャンクに分離）。ブラウザでの手動確認でも、テスト画像をアップロードして実際にハーフトーンQRへの変換・読み取り判定（解像度×ぼかしの9条件グリッド表示）・PNG書き出しが動作することを確認済み。ファインダーパターン（3隅の目印）は画像の柄がかぶっても保護されていることも目視確認した。コンソールエラーなし。

既知の制約: 書き出したPNGを等倍のままデコーダに渡すと失敗することがある（本方式はカメラ撮影時の光学的なぼけを前提とするため。等倍で読ませたい場合は通常のQRの方が安定する）。カラー画像は未対応（初期スコープ外、グレースケールのみ）。誤り訂正符号の再エンコード最適化（論文にある高度化）は未実装。E2Eテストはリポジトリに常設していない。

**技術方式（ハーフトーンQRの核心アイデア）**

- QRコードの1モジュールを3×3のサブモジュールに分解する
- 中央のサブモジュールだけは元のQRデータのビット（黒/白）を必ず維持する → ここが誤り訂正の対象データとして常に正しい値になるため、理論上は読み取りエラーを起こしにくい
- 残り8個のサブモジュールは自由に色を決められるので、埋め込みたい画像のその位置の濃淡（ハーフトーン処理した明暗）に合わせて塗る
- 結果として、QRコード全体が入力画像のシルエットに見えつつ、スキャンもできる

**要件**

- 入力
  - 画像アップロード（ドラッグ&ドロップ対応）
  - エンコードするURL/テキスト
- ハーフトーン変換
  - アップロード画像をグレースケール化 → 誤差拡散法（Floyd–Steinbergなど）でハーフトーン化し、QRの各モジュールの3×3サブモジュールに反映
  - 画像とQRモジュールのアライメント（拡大縮小・トリミング範囲）を調整できる
  - 「QRらしさ（ドットの主張の強さ）」を上下できるスライダー（画像再現度とスキャン安定性のトレードオフを調整するもの）
- プレビュー
  - 変換前（通常のQR）と変換後（ハーフトーンQR）を並べて比較表示
  - 設定変更をリアルタイムで反映
- スキャン耐性への配慮
  - 誤り訂正レベルは高め（H相当）を基本とし、必要に応じて自動調整
  - 中央固定ビットの仕組み上、理論上は読み取りエラーが起きにくい設計だが、実機での読み取りテストを促す注意書きを表示する
- 出力
  - PNG（高解像度オプションあり）
- データはすべてクライアントサイドで完結させ、サーバーへの送信を行わない（`apps/qr-code-designer/`と同じ方針）

### QRコードデザインツール（`apps/qr-code-designer/`）

単なるQRコード生成ではなく、ロゴ埋め込み・ドット形状・グラデーションなどに対応したデザイン性の高いQR生成ツール。既存の無料QRツールの多くが画一的な黒白ドットのデザインである点を差別化ポイントとする。名刺・パンフレット・店舗POPなどでの実用を想定。

sdd-managerで仕様作成〜実装〜検証のフルサイクルを実施。技術スタックはVite + React + TS、QRの生モジュール取得のみ`qrcode-generator`に依存し、描画は手書きSVG（プレビューSVGがそのままエクスポート元）。単体テスト92件通過、ビルド後JS 78KB gzip。ブラウザでの手動確認でも主要機能（プリセット切替・グラデーション・ロゴアップロード・PNG/SVG書き出し）が動作し、コンソールエラーなしを確認済み。

既知の制約: スキャン耐性チェックはアプリ内で実デコードせず静的推定（印刷前に実機テストを促す表示あり）。反転配色のプリセット（ネオン）は反転リトライに対応しない読み取り機では読めない場合がある。E2Eテストは一時検証のみでリポジトリには常設していない（今後CI化する場合はPlaywrightの追加が必要）。

**要件**

- 入力: URLまたは任意のテキスト
- QRコード生成
  - エラー訂正レベルを選択可能（L / M / Q / H）
  - ロゴを埋め込む場合はエラー訂正レベルHを自動選択（またはユーザーに推奨を表示）
- デザインカスタマイズ
  - ドットの形状（角ばった四角 / 丸 / 角丸）
  - ファインダーパターン（3隅の目印）の形状・色を個別に変更可能
  - 前景色・背景色は単色またはグラデーションに対応
  - 中央にロゴ画像をアップロードして埋め込み（サイズ・余白を調整可能）
- プレビュー: 入力・設定変更をリアルタイムで反映
- 出力
  - PNG（印刷用途を想定し高解像度オプションあり）とSVGの両方に対応
- スキャン耐性への配慮
  - ロゴ埋め込みやデザイン変更でスキャン精度が落ちる場合に警告表示、またはエラー訂正レベルを自動調整
- データはすべてクライアントサイドで完結させ、サーバーへの送信を行わない（プライバシー面を訴求ポイントにする）

**技術メモ**: QRコード生成ライブラリ（qrcode.js系など）でベースのQRデータを生成し、Canvas/SVG上でドット形状・色・ロゴを重ねて描画する方式を想定。ロゴ埋め込み時は中央付近のモジュールをマスクしてロゴ画像を重ねる。

## 保留

（なし）
